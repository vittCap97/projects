<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fernet.easy_fut5al">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@drawable/pallone"
        android:label="@string/app_name"
        android:roundIcon="@drawable/pallone"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RegistrationActivityAtleta"></activity>
        <activity android:name=".RegistrationActivityGestore"></activity>
        <activity
            android:name=".ActivityAtleta"
            android:label="@string/app_name">

        </activity>
        <activity
            android:name=".ActivityGestore"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".ActivityAutentificazione"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login">

        </activity>

    </application>

</manifest>